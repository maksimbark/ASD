#include <bits/stdc++.h>

using namespace std;
vector<vector<int>> matrix(100, vector<int>(100));

int main() {
    ifstream fin("input.txt");
    ofstream fout("output.txt");
    int n, m;
    fin >> n >> m;
    return 0;
}